# active = off, a preset name (from rune_palette.cfg, like "purple"), "custom", or "rainbow"
active=off

# only used when active=custom
custom_body=#12356B
custom_glow=#FFFFFF
custom_boost=1.0

# only used when active=rainbow (emissive boost for the rainbow fill)
rainbow_boost=3
